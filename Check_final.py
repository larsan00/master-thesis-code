from Check_animation import *
from Check_equations import *
from Check_Initialize import *

#simulation and animation parameter
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
dx = 0.01 # in meters
dt = 0.00001 # in seconds
simulationTime = 100 # in seconds
animation_steps = 100 # in frames

#initialize the grid of the room
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
steps, h, l, b = init_grid(simulationTime, dx, dt)

T_start = 273.15 + 20 # Initial temperature in K
T = T_start * np.ones((h, l, b)) 
h_newton = 5 # Heat transfer coefficient of air 


#initialize the constants
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
c_spec, D, alpha_, R_s, v, g = init_const()

#initialize the density
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
rho_start = 1.18 

rho, c, vx, vy, vz = init_rho_c_v(rho_start, h, l, b)
dT_Wall, A, B, C, D, Alpha, dt_dx = dt / (c_spec * 1.18 * dx), 2 * R_s / dx, v / dx**2, (-v / 3) / (4 * dx**2), D / dx, alpha_/ dx, dt / dx


#Check 1: Fourier law
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#T[:,0,:], T[:,-1,:] = 273.15 + 15, 273.15 + 25
#g = 0

#Check 2: barometric formula
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#rho = np.ones((h, l, b)) * rho_start
#h_newton, Alpha = 0, 0
#v *= 1000

#Check 3: speed of sound
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#rho[rho.shape[0]//10, rho.shape[1]//2, rho.shape[2]//2] = 1.19
#h_newton, g = 0, 0

#Check 4: Diffusion
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#c = np.zeros((h, l, b))
#c[h//2, l//2, b//2] = 1

#Check 5: Conservation laws
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#g, h_newton = 0, 0

#c = np.zeros((h, l, b))
#c[h//2, l//2, b//2] = 1
#rho[h//2, l//2, b//2] = 1.19

# Run the main simulation loop
# -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
c, rho, T, vx, vy, vz = main_gpu(h, l, b, dx, dt, steps, animation_steps, c, rho, T, dt_dx, A, B, C, vx, vy, vz, h_newton, dT_Wall, Alpha, g, D)


# Save the final results
#---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
f_name = '_sim_dx_'+str(dx)+'.'

#np.save('vx' + f_name + "npy", vx)
#np.save('vy' + f_name + "npy", vy)
#np.save('vz' + f_name + "npy", vz)
#np.save(f"T_h_{h_newton}{f_name}npy", T)
#np.save('rho' + f_name + "npy", rho)
#np.save('c' + f_name + "npy", c)
